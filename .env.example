# ========== CONVERSIÓN PDF → MARKDOWN (SISTEMA ROBUSTO) ==========
# Directorios locales (ignorados por Git - BYOS policy)
SOURCES_LOCAL_ORIGINALS=sources_local/originals
SOURCES_LOCAL_CONVERTED=sources_local/converted
SOURCES_LOCAL_METADATA=sources_local/metadata
SOURCES_LOCAL_REPORTS=sources_local/reports

# Directorio legacy (deprecated - usar sources_local/)
MARKDOWN_OUTPUT_DIR=sources/markdown_outputs

# Dispositivo para marker-sdk: cpu, cuda (NVIDIA GPU), mps (Mac M1/M2/M3)
# Recomendado: cpu para compatibilidad, cuda/mps si tienes GPU
MARKER_DEVICE=cpu

# Forzar OCR en todos los PDFs (útil para documentos escaneados)
# Valores: true, false
MARKER_FORCE_OCR=false

# Batch size para procesamiento de páginas (ajustar según RAM disponible)
MARKER_BATCH_SIZE=10

# Workers para procesamiento paralelo
MARKER_WORKERS=4

# Idiomas para OCR (separados por coma)
PDF_LANG=en,es

# ========== LLM (Para Fase 2 - Generación Automatizada) ==========
# Actualmente la generación es manual con LLMs web (Gemini, GPT-4, Claude)
# Estas variables se usarán cuando se implemente generación automatizada

# API Keys para LLMs comerciales (opcional para generación manual)
# OPENAI_API_KEY=sk-proj-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=AIza...

# Configuración Ollama (para futuro uso local)
# OLLAMA_BASE_URL=http://localhost:11434
# LLM_MODEL=gemma2:9b
# LLM_TEMPERATURE=0.3

# ========== EMBEDDINGS Y BÚSQUEDA (Para Fase 2+) ==========
# Sistema de embeddings y búsqueda vectorial no implementado aún
# Estas variables se usarán en Fase 2

# EMBEDDING_MODEL=embeddinggemma:300m
# EMBEDDING_DIM=300
# LANCEDB_PATH=./data/lancedb
# TOP_K=10
# SEARCH_TYPE=hybrid
# CONFIDENCE_THRESHOLD=0.10

# ========== VALIDACIÓN Y QA ==========
# Confidence score mínimo para ingestar chunks
# Chunks con score menor serán rechazados
MIN_CONFIDENCE_SCORE=0.70

# Reliability level mínimo: verified, high, medium, low
MIN_RELIABILITY_LEVEL=medium

# Threshold de similitud coseno para detectar duplicados (0.0-1.0)
# Valores típicos: 0.85-0.95
DUPLICATE_THRESHOLD=0.85

# Porcentaje de muestra para validación semántica (0.0-1.0)
SEMANTIC_VALIDATION_SAMPLE=0.2

# ========== CHUNKING ==========
# Tamaño mínimo de chunk en tokens
MIN_CHUNK_SIZE=150

# Tamaño máximo de chunk en tokens
MAX_CHUNK_SIZE=500

# Tamaño típico objetivo
TYPICAL_CHUNK_SIZE=350

# Overlap entre chunks (porcentaje)
CHUNK_OVERLAP=0.15

# ========== TAXONOMÍA Y DOMINIO ==========
# Categorías válidas (separadas por coma)
VALID_CATEGORIES=BIO,PROC,MAT,OPER,PROD

# Idioma principal del dataset
DATASET_LANGUAGE=es

# Dominio específico
DATASET_DOMAIN=vermicompostaje_domestico

# ========== SISTEMA ==========
# Nivel de logging: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Archivo de logs (comentar para mostrar solo en consola)
LOG_FILE=logs/vermi-rag.log

# Workers para procesamiento paralelo (CPU cores a usar)
# 0 = auto-detectar, N = usar N workers
MAX_WORKERS=4

# Timeout para operaciones de red (segundos)
NETWORK_TIMEOUT=30

# ========== PATHS ==========
# Directorio raíz del proyecto (auto-detectado, no cambiar a menos que sea necesario)
PROJECT_ROOT=.

# Directorio de fuentes (PDFs, MDs - ignorados por Git)
SOURCES_DIR=sources

# Directorio del dataset (chunks JSONL)
DATASET_DIR=dataset

# Directorio de scripts
SCRIPTS_DIR=scripts

# Directorio de documentación
DOCS_DIR=docs

# ========== DESARROLLO Y DEBUG ==========
# Modo desarrollo: activa validaciones extra y logs verbosos
DEV_MODE=false

# Cache de modelos y embeddings (para evitar recalcular)
ENABLE_CACHE=true
CACHE_DIR=.cache

# Mostrar barras de progreso en terminal
SHOW_PROGRESS=true

# ========== GITHUB ACTIONS / CI ==========
# Estas variables se configuran automáticamente en CI, no editar localmente
# CI=true
# GITHUB_TOKEN=ghp_...
# RUNNER_OS=Linux
